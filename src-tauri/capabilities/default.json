{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for symbolPop windows",
  "windows": ["settings", "quick"],
  "permissions": [
    "core:default",
    "opener:default",
    "global-shortcut:default",
    "core:app:allow-version",
    "updater:default"
  ]
}
